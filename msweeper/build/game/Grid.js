export default class Grid {
  constructor(x, y, img = "../sprites/square.png") {
    //for image default is square.png
    this.x = x;
    this.y = y;
    this.img = img;
  }

  //somehow render it here
}
